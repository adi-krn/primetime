// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["m.media-amazon.com", "another-domain.com", "example.com"],
  },
};

export default nextConfig;
